{
  "name": "urge-guard",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "web-ext run -s dist --firefox=firefoxdeveloperedition"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@types/webextension-polyfill": "^0.12.5",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "web-ext": "^8.4.0"
  }
}
